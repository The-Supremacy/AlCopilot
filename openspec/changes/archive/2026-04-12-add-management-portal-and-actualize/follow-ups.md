# Follow-Up References

## Deferred Access Control

Recommended follow-up change identifier:

- `integrate-management-portal-access-control`

Suggested scope:

- define manager and future admin access model
- add portal session UX for operators when application-level access control is introduced
- replace placeholder user menu behavior in the management portal

## Deferred Design-System Expansion

Recommended follow-up change identifier:

- `expand-management-portal-design-system`

Suggested scope:

- extract stable visual primitives from the management portal into a broader shared UI package
- introduce documented design tokens only after cross-portal reuse is real
- revisit branding and broader theming once product priorities justify it
